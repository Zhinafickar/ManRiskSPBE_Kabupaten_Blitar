'use server';

// This file is intentionally left mostly blank.
// User management actions have been moved to client-side services
// in user-service.ts to avoid issues with using the Firebase Client SDK
// in Server Actions, which was causing "client is offline" errors.
