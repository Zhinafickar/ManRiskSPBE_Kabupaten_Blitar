rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This rule is for development and allows any authenticated user
    // to read and write all data. Secure your rules before production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
